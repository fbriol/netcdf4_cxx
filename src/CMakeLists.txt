FILE(GLOB SOURCES "*.cpp")

ADD_LIBRARY(netcdf4_cxx SHARED ${SOURCES})
TARGET_LINK_LIBRARIES(netcdf4_cxx ${NETCDF_C_LIBRARY})
INSTALL(TARGETS netcdf4_cxx DESTINATION lib)

INSTALL(FILES ${headers} DESTINATION include)